# Generated: every entry is an absolute path into one machine's checkout and Gradle cache.
# Rebuild with: SUBSTRAIT_JAVA_DIR=<checkout> bash gen/make_classpath.sh
gen/classpath.txt
gen/out/

# The probe environment (venvs, the compiled substrait-go probe), built by probe/setup.sh.
.probe-env/

__pycache__/
*.pyc
.DS_Store

# Where OUT= puts a kept run of probe/replay_column.sh: the column, the raw output and the record.
# CI uploads that directory as an artifact; locally it is scratch.
run/

# Protobuf bindings for tests/relations, written by tests/relations/bootstrap.sh.
tests/relations/.bindings/
